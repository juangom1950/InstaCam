<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bitfontain.juangomez.com.instacam" >

    <!-- Our own app is trying to get something from file and read it, so we need to ad permission to do that -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Add facebook Internet permissions to be able to connect to Facebook's servers. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FacebookHashKey"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Add the application ID as meta-data. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/fb_app_id" />

        <activity
            android:name=".MainActivity"
            android:label="MainActivity" >
        </activity>
        <activity
            android:name=".NewPhotoActivity"
            android:label="@string/title_activity_new_photo" >
        </activity>

        <!--Define your Activity in the AndroidManifest.xml. If it is the first Activity of your app,
            you should also add an intent-filter that responds to the action android.intent.action.MAIN.-->
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Define FacebookActivity as another Activity that belongs to your app. It handles most of the
        configuration changes itself. You need to mention that using the configChanges attribute-->
        <activity android:name="com.facebook.FacebookActivity"
                  android:configChanges=
                    "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
                  android:theme="@android:style/Theme.Translucent.NoTitleBar"
                  android:label="@string/app_name" />
    </application>

</manifest>
